<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.CasaHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cor primária do app (laranja) -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryContainer">@color/meu_laranja_variante</item>

        <item name="colorSecondary">@color/meu_laranja_variante</item>

        <item name="android:colorBackground">@color/meu_fundo_branco</item>
        <item name="android:textColor">@color/minha_letra_preta</item>
        <item name="materialButtonStyle">@style/Widget.Material3.Button</item>
    </style>
</resources>
